# Usando /api relativo para que el proxy de Vite lo intercepte y reenvíe al backend
# El proxy en vite.config.js redirige /api → https://localhost:7156/api
VITE_API_URL=/api
VITE_SIGNALR_HUB_URL=/hubs/results
